pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh 'date'
                echo 'Hi from VS Code'
            }
        }
    }
}